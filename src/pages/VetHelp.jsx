import React from "react";
import SectionTitle from "../components/SectionTitle";
import MainLayout from "../layouts/MainLayout";

const VetHelp = () => {
  return (
    <MainLayout>
      <SectionTitle title={"VetHelp"} />
    </MainLayout>
  );
};

export default VetHelp;
